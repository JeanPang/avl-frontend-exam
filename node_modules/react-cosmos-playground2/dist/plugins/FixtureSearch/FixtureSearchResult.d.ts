import { FixtureId } from 'react-cosmos-shared2/renderer';
declare type Props = {
    cleanFixturePath: string;
    fixtureId: FixtureId;
    active: boolean;
    onSelect: (fixtureId: FixtureId, revealFixture: boolean) => unknown;
};
export declare function FixtureSearchResult({ cleanFixturePath, fixtureId, active, onSelect }: Props): JSX.Element;
export {};
