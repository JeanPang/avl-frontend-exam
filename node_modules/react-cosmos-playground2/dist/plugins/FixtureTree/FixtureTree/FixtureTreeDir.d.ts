import { FixtureId } from 'react-cosmos-shared2/renderer';
import { FixtureNode } from '../../../shared/fixtureTree';
declare type Props = {
    node: FixtureNode;
    parents: string[];
    isExpanded: boolean;
    selectedFixtureId: null | FixtureId;
    onToggle: () => unknown;
};
export declare function FixtureTreeDir({ node, parents, selectedFixtureId, isExpanded, onToggle }: Props): JSX.Element;
export {};
