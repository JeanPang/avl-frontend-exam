import { FixtureId } from 'react-cosmos-shared2/renderer';
declare type Props = {
    parents: string[];
    item: FixtureId;
    itemName: string;
    selectedFixtureId: null | FixtureId;
    onSelect: (path: FixtureId) => unknown;
};
export declare function FixtureTreeItem({ parents, item, itemName, selectedFixtureId, onSelect }: Props): JSX.Element;
export {};
