import React from 'react';
import { TreeNode } from '../tree';
export declare type TreeExpansion = {
    [nodePath: string]: boolean;
};
declare type Props<Item> = {
    node: TreeNode<Item>;
    parents?: string[];
    treeExpansion: TreeExpansion;
    renderDir: (args: {
        node: TreeNode<Item>;
        parents: string[];
        isExpanded: boolean;
        onToggle: () => unknown;
    }) => React.ReactNode;
    renderItem: (args: {
        parents: string[];
        item: Item;
        itemName: string;
    }) => React.ReactNode;
    onTreeExpansionChange: (newTreeExpansion: TreeExpansion) => unknown;
};
export declare function TreeView<Item>({ node, parents, treeExpansion, renderDir, renderItem, onTreeExpansionChange }: Props<Item>): JSX.Element;
export {};
