declare type BaseOption = {
    value: string;
    label: string;
};
declare type Props<Option extends BaseOption> = {
    id?: string;
    testId?: string;
    options: Option[];
    value: string;
    onChange: (newValue: Option) => unknown;
};
export declare function Select<Option extends BaseOption>({ id, testId, options, value, onChange }: Props<Option>): JSX.Element;
export {};
