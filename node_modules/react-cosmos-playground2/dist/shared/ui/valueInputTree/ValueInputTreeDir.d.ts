import { ValueNode } from './shared';
declare type Props = {
    node: ValueNode;
    parents: string[];
    isExpanded: boolean;
    onToggle: () => unknown;
};
export declare function ValueInputTreeDir({ node, parents, isExpanded, onToggle }: Props): JSX.Element;
export {};
