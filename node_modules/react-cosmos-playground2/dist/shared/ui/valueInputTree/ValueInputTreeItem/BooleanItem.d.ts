declare type Props = {
    id: string;
    label: string;
    value: boolean;
    onChange: (newValue: boolean) => unknown;
};
export declare function BooleanItem({ id, label, value, onChange }: Props): JSX.Element;
export {};
