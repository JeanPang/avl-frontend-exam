declare type Props = {
    id: string;
    label: string;
    value: number;
    onChange: (newValue: number) => unknown;
};
export declare function NumberItem({ id, label, value, onChange }: Props): JSX.Element;
export {};
