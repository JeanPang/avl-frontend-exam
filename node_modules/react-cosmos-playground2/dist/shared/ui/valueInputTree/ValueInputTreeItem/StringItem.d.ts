declare type Props = {
    id: string;
    label: string;
    value: string;
    onChange: (newValue: string) => unknown;
};
export declare function StringItem({ id, label, value, onChange }: Props): JSX.Element;
export {};
