import { FixtureStateValues } from 'react-cosmos-shared2/fixtureState';
import { TreeItemValue } from '../shared';
declare type Props = {
    treeId: string;
    values: FixtureStateValues;
    parents: string[];
    item: TreeItemValue;
    itemName: string;
    onValueChange: (values: FixtureStateValues) => unknown;
};
export declare function ValueInputTreeItem({ treeId, values, parents, item, itemName, onValueChange }: Props): JSX.Element;
export declare const ItemContainer: import("styled-components").StyledComponent<"div", any, {}, never>;
export {};
