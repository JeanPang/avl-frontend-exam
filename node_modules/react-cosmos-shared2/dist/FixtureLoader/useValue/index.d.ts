import { FixtureStateValueType } from '../../fixtureState';
import { SetValue } from './shared';
declare type Opts<T extends FixtureStateValueType> = {
    defaultValue: T;
};
export declare function useValue<T extends FixtureStateValueType>(inputName: string, { defaultValue }: Opts<T>): [T, SetValue<T>];
export {};
